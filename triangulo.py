import math

print('\n  TRIANGULO RETANGULO\n')

cat_op = float(input("Valor do cateto oposto:"))
cat_ad = float(input('Valor do cateto adjacente'))

hip = math.sqrt(pow(cat_ad, 2) + pow(cat_op, 2))


print('\nHipotenusa = {:.2f} e {:.2f}'.format(hip, hip2))



























